# Agents Usage Guide

各 `agent` は以下の5項目を必ず持つ。

- 入力
- 出力
- 完了条件
- レビュー観点
- 更新対象ファイル

## 呼び出しルール
- 毎回 agent 名を明示して依頼する。
- 対象工程と入力資料を最初に指定する。
- 出力先は `docs/`, `src/`, `tests/` のどこかに固定する。

## 例
`01-basic-design-agent.md に従って、要件定義を元に基本設計を作成して`
`04-unit-test-agent.md に従って、対象機能の単体テストと試験設計書を更新して`
